import React from 'react';
import Dashboard from './Dashboard';

// Simple wrapper to use the existing Dashboard component
const StudentDashboard: React.FC = () => {
  return <Dashboard />;
};

export default StudentDashboard;